import styled from 'styled-components';

const StyleFooter = styled.div`
  .Main-Footer {
    color: white;
    padding-left: 1%;
    position: fixed;
    bottom: 0;
  }
`;

export default StyleFooter;
